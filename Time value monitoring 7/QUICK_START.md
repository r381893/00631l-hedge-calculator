# 快速開始 (Quick Start)

## 1. 環境設定 (Setup)

### Windows 使用者
1. **安裝 Python**: 請至 [Python 官網](https://www.python.org/downloads/) 下載並安裝 Python (建議 3.10 以上)。
2. **安裝套件**: 雙擊目錄下的 `install_requirements.bat` (若無則手動執行指令) 或開啟 CMD 輸入:
   ```bash
   pip install -r requirements.txt
   ```
3. **啟動程式**: 雙擊 `start_monitor.bat` 即可執行。

### Mac 使用者
1. **安裝套件**: 開啟終端機 (Terminal) 並執行:
   ```bash
   pip install -r requirements.txt
   ```
2. **啟動程式**: 雙擊 `start_monitor.command` 或執行:
   ```bash
   streamlit run app.py
   ```

## 2. 首次使用 (First Run)

1. 程式啟動後，瀏覽器會自動開啟 (預設 http://localhost:8501)。
2. 在左側側邊欄 (Sidebar) 設定：
   - **環境選擇**: 建議先選擇「測試環境 (Test)」。
   - **API 連接**: 點擊「🔌 連接富邦 API」。
3. 連接成功後，即可開始使用監控功能。

## 3. 詳細設定

關於憑證申請與詳細 API 設定，請參考 [FUBON_SETUP.md](FUBON_SETUP.md)。
